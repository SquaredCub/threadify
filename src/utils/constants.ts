// . MAX VALUES
// . ----------
export const MAX_ITERATIONS = 8000;
export const MAX_POINTS = 500;

// . DEFAULT VALUES
// . --------------
export const DEFAULT_ITERATIONS = 2000;
export const DEFAULT_POINTS = 200;

export const DEFAULT_FADE = 25;
export const DEFAULT_MIN_DISTANCE = 10;

export const DEFAULT_CANVAS_WIDTH = 600;
export const DEFAULT_CANVAS_HEIGHT = 600;

export const DEFAULT_IMAGE_SIZE = 100;
